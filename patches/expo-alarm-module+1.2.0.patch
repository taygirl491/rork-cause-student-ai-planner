diff --git a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Alarm.java b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Alarm.java
index d12270b..8f4424a 100644
--- a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Alarm.java
+++ b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Alarm.java
@@ -32,6 +32,7 @@ public class Alarm implements Cloneable {
     String description;
     boolean repeating;
     boolean active;
+    String sound;
     
     /**
      * Show dismiss button in notification. It is false by default.
@@ -55,7 +56,7 @@ public class Alarm implements Cloneable {
     String dismissText;
     String snoozeText;
 
-    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
+    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText, String sound) {
         this.uid = uid;
         this.days = days;
         this.hour = hour;
@@ -69,6 +70,7 @@ public class Alarm implements Cloneable {
         this.active = active;
         this.dismissText = dismissText;
         this.snoozeText = snoozeText;
+        this.sound = sound;
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
           this.date = date;
         }
diff --git a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 0aa62bb..890567a 100644
--- a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -176,7 +176,9 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    String sound = alarm.hasKey("sound") ? alarm.getString("sound") : "default";
+
+    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText, sound);
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
diff --git a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Manager.java b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Manager.java
index 569d3cb..a553030 100644
--- a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Manager.java
+++ b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Manager.java
@@ -114,7 +114,9 @@ public class Manager {
     static void start(Context context, String alarmUid) {
         activeAlarmUid = alarmUid;
         sound = new Sound(context);
-        sound.play("default");
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        String soundName = (alarm != null && alarm.sound != null) ? alarm.sound : "default";
+        sound.play(soundName);
 
         Log.d(TAG, "Starting " + activeAlarmUid);
     }
diff --git a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Sound.java b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Sound.java
index 9546c85..56739dd 100644
--- a/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Sound.java
+++ b/node_modules/expo-alarm-module/android/src/main/java/com/expoalarmmodule/Sound.java
@@ -100,7 +100,7 @@ class Sound {
     private Uri getSoundUri(String soundName) {
         Uri soundUri;
         if (soundName.equals("default")) {
-            soundUri = Settings.System.DEFAULT_RINGTONE_URI;
+            soundUri = Settings.System.DEFAULT_ALARM_ALERT_URI;
         } else {
             int resId;
             if (context.getResources().getIdentifier(soundName, "raw", context.getPackageName()) != 0) {
diff --git a/node_modules/expo-alarm-module/ios/Alarms.swift b/node_modules/expo-alarm-module/ios/Alarms.swift
index 9ad7803..3c4ddad 100644
--- a/node_modules/expo-alarm-module/ios/Alarms.swift
+++ b/node_modules/expo-alarm-module/ios/Alarms.swift
@@ -31,11 +31,18 @@ class Alarms: Codable {
     
     func add(_ alarm: Alarm) {
         alarms.append(alarm)
-        let newIndex = alarms.firstIndex { $0.uid == alarm.uid }!
-        Store.shared.save(self, notifying: alarm, userInfo: [
-            Alarm.changeReasonKey: Alarm.added,
-            Alarm.newValueKey: newIndex
-        ])
+        if let newIndex = alarms.firstIndex(where: { $0.uid == alarm.uid }) {
+            Store.shared.save(self, notifying: alarm, userInfo: [
+                Alarm.changeReasonKey: Alarm.added,
+                Alarm.newValueKey: newIndex
+            ])
+        } else {
+            print("Error: Could not find index of added alarm \(alarm.uid)")
+            Store.shared.save(self, notifying: alarm, userInfo: [
+                Alarm.changeReasonKey: Alarm.added,
+                Alarm.newValueKey: alarms.count - 1
+            ])
+        }
     }
     
     func remove(_ uid: String) {
diff --git a/node_modules/expo-alarm-module/ios/ExpoAlarmModule.swift b/node_modules/expo-alarm-module/ios/ExpoAlarmModule.swift
index 85af3bf..9395ec2 100644
--- a/node_modules/expo-alarm-module/ios/ExpoAlarmModule.swift
+++ b/node_modules/expo-alarm-module/ios/ExpoAlarmModule.swift
@@ -42,7 +42,11 @@ class ExpoAlarmModule: NSObject, UNUserNotificationCenterDelegate, AVAudioPlayer
 
     @objc(set:withResolver:withRejecter:)
     func set(alarm: NSDictionary, resolve:RCTPromiseResolveBlock, reject:RCTPromiseRejectBlock) -> Void {
-        let alarmToUse = Alarm(dictionary: alarm as! NSMutableDictionary);
+        guard let mutableAlarm = alarm.mutableCopy() as? NSMutableDictionary else {
+            reject("error", "Could not convert alarm to NSMutableDictionary", nil)
+            return
+        }
+        let alarmToUse = Alarm(dictionary: mutableAlarm);
 
         manager.schedule(alarmToUse);    
 
@@ -193,7 +197,10 @@ class ExpoAlarmModule: NSObject, UNUserNotificationCenterDelegate, AVAudioPlayer
             },
             nil)
         
-        guard let filePath = Bundle.main.path(forResource: soundName, ofType: "mp3") else {fatalError()}
+        guard let filePath = Bundle.main.path(forResource: soundName, ofType: "mp3") else {
+            print("Error: Could not find sound file \(soundName).mp3")
+            return
+        }
         let url = URL(fileURLWithPath: filePath)
         
         do {
diff --git a/node_modules/expo-alarm-module/ios/Manager.swift b/node_modules/expo-alarm-module/ios/Manager.swift
index 7bbc1a9..d6ed52a 100644
--- a/node_modules/expo-alarm-module/ios/Manager.swift
+++ b/node_modules/expo-alarm-module/ios/Manager.swift
@@ -27,10 +27,10 @@ class Manager {
     
 
     func enable(_ uid: String) {
-        let alarm: Alarm! = self.getAlarm(uid) ?? nil;
+        let alarm = self.getAlarm(uid)
         
         // Only enables if the alarm already exists and is disabled.
-        if((alarm != nil) && !alarm.active) {
+        if let alarm = alarm, !alarm.active {
             // Creates the notification (the alarm).
             scheduler.setNotification(alarm: alarm)
             
@@ -44,10 +44,10 @@ class Manager {
         // Stops any sound of alarm that is playing.
         self.stop()
         
-        let alarm: Alarm! = self.getAlarm(uid) ?? nil
+        let alarm = self.getAlarm(uid)
         
         // Only disables if the alarm already exists and is enabled.
-        if((alarm != nil) && alarm.active) {
+        if let alarm = alarm, alarm.active {
             // Cancels the notification.
             scheduler.cancelNotification(ByUUIDStr: uid)
             
diff --git a/node_modules/expo-alarm-module/ios/NotificationScheduler.swift b/node_modules/expo-alarm-module/ios/NotificationScheduler.swift
index 4772edc..751e262 100644
--- a/node_modules/expo-alarm-module/ios/NotificationScheduler.swift
+++ b/node_modules/expo-alarm-module/ios/NotificationScheduler.swift
@@ -49,7 +49,7 @@ class NotificationScheduler : NotificationSchedulerDelegate
             requests in
             print(requests)
             let alarms = Store.shared.alarms
-            let uuidNotificationsSet = Set(requests.map({$0.content.userInfo["uid"] as! String}))
+            let uuidNotificationsSet = Set(requests.compactMap({ $0.content.userInfo["uid"] as? String }))
             let uuidAlarmsSet = alarms.uids
             let uuidDeltaSet = uuidAlarmsSet.subtracting(uuidNotificationsSet)
             
@@ -78,7 +78,11 @@ class NotificationScheduler : NotificationSchedulerDelegate
         //scheduling date is eariler than current date
         if date < now {
             //plus one day, otherwise the notification will be fired righton
-            notificationDates.append((calendar as NSCalendar).date(byAdding: NSCalendar.Unit.day, value: 1, to: date, options:.matchStrictly)!)
+            if let nextDate = (calendar as NSCalendar).date(byAdding: NSCalendar.Unit.day, value: 1, to: date, options:.matchStrictly) {
+                notificationDates.append(nextDate)
+            } else {
+                notificationDates.append(date.addingTimeInterval(24 * 3600))
+            }
         } else {
             notificationDates.append(date)
         }
@@ -88,8 +92,10 @@ class NotificationScheduler : NotificationSchedulerDelegate
     
     static func correctSecondComponent(date: Date, calendar: Calendar = Calendar(identifier: Calendar.Identifier.gregorian)) -> Date {
         let second = calendar.component(.second, from: date)
-        let d = (calendar as NSCalendar).date(byAdding: NSCalendar.Unit.second, value: -second, to: date, options:.matchStrictly)!
-        return d
+        if let d = (calendar as NSCalendar).date(byAdding: NSCalendar.Unit.second, value: -second, to: date, options:.matchStrictly) {
+            return d
+        }
+        return date
     }
     
     func setNotification(alarm: Alarm) {
@@ -122,13 +128,18 @@ class NotificationScheduler : NotificationSchedulerDelegate
     
     func setNotificationForSnooze(ringtoneName: String, snoozeMinute: Int, uid: String) {
         let currentAlarm = alarms.getAlarm(ByUUIDStr: uid);
-        if(currentAlarm != nil) {
+        if let alarm = currentAlarm {
             let calendar = Calendar(identifier: Calendar.Identifier.gregorian)
             let now = Date()
-            let snoozeDate = (calendar as NSCalendar).date(byAdding: NSCalendar.Unit.minute, value: snoozeMinute, to: now, options:.matchStrictly)!
-            setNotification(alarm: currentAlarm!)
+            if let snoozeDate = (calendar as NSCalendar).date(byAdding: NSCalendar.Unit.minute, value: snoozeMinute, to: now, options:.matchStrictly) {
+                // The snoozeDate isn't actually used in this version of the code, 
+                // but we might as well calculate it safely if the original intent was to use it or verify it.
+                setNotification(alarm: alarm)
+            } else {
+                setNotification(alarm: alarm)
+            }
         } else {
-            print("Error when setting notification for snooze")
+            print("Error when setting notification for snooze: Alarm not found for uid \(uid)")
         }
     }
     
@@ -139,10 +150,10 @@ class NotificationScheduler : NotificationSchedulerDelegate
     func updateNotification(ByUUIDStr uid: String, date: Date, ringtoneName: String, snoonzeEnabled: Bool) {
         cancelNotification(ByUUIDStr: uid)
         let currentAlarm = alarms.getAlarm(ByUUIDStr: uid);
-        if(currentAlarm != nil) {
-            setNotification(alarm: currentAlarm!)
+        if let alarm = currentAlarm {
+            setNotification(alarm: alarm)
         } else {
-            print("Error updating notification")
+            print("Error updating notification: Alarm not found for uid \(uid)")
         }
     }
     
diff --git a/node_modules/expo-alarm-module/lib/typescript/src/types/Alarm.types.d.ts b/node_modules/expo-alarm-module/lib/typescript/src/types/Alarm.types.d.ts
index 95fa365..036424e 100644
--- a/node_modules/expo-alarm-module/lib/typescript/src/types/Alarm.types.d.ts
+++ b/node_modules/expo-alarm-module/lib/typescript/src/types/Alarm.types.d.ts
@@ -51,5 +51,6 @@ export type AlarmSettings = {
      * If the alarm needs to repeat.
      */
     repeating?: boolean | undefined;
+    sound?: string | undefined;
 };
 //# sourceMappingURL=Alarm.types.d.ts.map
\ No newline at end of file
diff --git a/node_modules/expo-alarm-module/src/models/Alarm.tsx b/node_modules/expo-alarm-module/src/models/Alarm.tsx
index ecc2b74..fc5bc25 100644
--- a/node_modules/expo-alarm-module/src/models/Alarm.tsx
+++ b/node_modules/expo-alarm-module/src/models/Alarm.tsx
@@ -14,6 +14,7 @@ class Alarm {
   repeating?: boolean | undefined;
   active?: boolean | undefined;
   day?: string | Date | number[] | undefined;
+  sound?: string | undefined;
 
   constructor(params: any = null) {
     this.uid = getParam(params, 'uid');
@@ -29,6 +30,7 @@ class Alarm {
     this.repeating = getParam(params, 'repeating');
     this.active = getParam(params, 'active');
     this.day = getParam(params, 'day');
+    this.sound = getParam(params, 'sound');
   }
 
   static getEmpty() {
diff --git a/node_modules/expo-alarm-module/src/types/Alarm.types.ts b/node_modules/expo-alarm-module/src/types/Alarm.types.ts
index d97354b..cf35c49 100644
--- a/node_modules/expo-alarm-module/src/types/Alarm.types.ts
+++ b/node_modules/expo-alarm-module/src/types/Alarm.types.ts
@@ -63,4 +63,8 @@ export type AlarmSettings = {
    * If the alarm needs to repeat.
    */
   repeating?: boolean | undefined;
+  /**
+   * Sound to play. Use 'default' for system alarm sound.
+   */
+  sound?: string | undefined;
 };
